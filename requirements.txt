# Core dependencies
requests>=2.28.0
cryptography>=3.4.8

# Windows-specific dependencies (optional for CI)
pywin32>=306; sys_platform == "win32"

# Development and packaging
pyinstaller>=5.0
setuptools>=65.0.0

# Optional: For enhanced logging and configuration
configparser>=5.0.0 